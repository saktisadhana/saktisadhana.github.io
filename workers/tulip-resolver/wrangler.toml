name = "tulip-pm-resolver"
main = "worker.js"
compatibility_date = "2024-11-01"

# The flag is NOT stored here. Set it as a secret:
#   wrangler secret put FLAG
# or in the Cloudflare dashboard: Worker -> Settings -> Variables -> Add secret (FLAG).
